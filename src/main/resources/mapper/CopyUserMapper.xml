<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.springmvc.dao.CopyOfUserDAO">
	<insert id="insertUser" parameterType="User" keyProperty="id">
		insert into user( state, nickname) values
		(#{state}, #{nickname})
	</insert>
	
	
	<insert id="iser5t" parameterType="User" keyProperty="id">
		insert into user( state, nickname) values
		(#{state}, #{nickname})
	</insert>
	
	
	<select id="selectNmae">
	select * from user
	</select>
</mapper>
